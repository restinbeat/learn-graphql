spring:
  application:
    name: learn-springboot-graphql

  threads:
    virtual:
      enabled: true

  graphql:
    graphiql:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true